import os
import sys

ROOT_PATH = os.path.join(os.path.dirname(sys.argv[0]), "StarRailTools")

CONFIG_PATH = os.path.join(ROOT_PATH, "config")

LOG_PATH = os.path.join(ROOT_PATH, "log")

REQUEST_TIMEOUT = 3

MENU_BANNER_LENGTH = 40

APP_NAME = "StarRailTools"
